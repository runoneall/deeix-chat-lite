zerops:
    - setup: docker
      build:
          deployFiles:
              - Dockerfile

      run:
          base: docker@latest
          prepareCommands:
              - docker build -t deeix-chat-lite:v1 .
          initCommands:
              - docker rm -f deeix-chat-lite
          start: docker run --name deeix-chat-lite -e GIN_MODE -e JWT_SECRET -e DATA_ENCRYPTION_KEY -e CORS_ALLOW_ORIGIN
          ports:
              - port: 8080
                httpSupport: true
